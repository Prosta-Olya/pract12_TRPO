<Page x:Class="pract12_TRPO.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Loaded="Page_Loaded"
      Title="MainPage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button Style="{StaticResource MaterialDesignRaisedButton}"
                    Click="go_form"
                    Content="Добавить"/>
            <Button Style="{StaticResource MaterialDesignRaisedButton}"
                    Click="remove"
                    Content="Удалить"/>
            <Button Style="{StaticResource MaterialDesignRaisedButton}"
        Click="roles"
        Content="Роли"/>
            <Button Style="{StaticResource MaterialDesignRaisedButton}"
Click="Button_Click"
Content="Группы"/>
        </StackPanel>
        <ListView Grid.Row="1"
                  ItemsSource="{Binding service.Students}"
                  MouseDoubleClick="Edit"
                  SelectedItem="{Binding student}"
                  Style="{StaticResource MaterialDesignListBox}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="0">
                        <TextBlock Text="{Binding Login}" Style="{StaticResource MaterialDesignBody1TextBlock}"/>
                        <TextBlock Text="{Binding Name}" Style="{StaticResource MaterialDesignBody2TextBlock}"/>
                        <TextBlock Text="{Binding Email}" Style="{StaticResource MaterialDesignBody2TextBlock}"/>
                        <TextBlock Text="{Binding CreatedAt, StringFormat={}{0:dd.MM.yyyy}}" 
                                   Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                   Foreground="{DynamicResource SecondaryHueMidForegroundBrush}"/>
                        <TextBlock>Данные из профиля</TextBlock>
                        <TextBlock Style="{StaticResource MaterialDesignBody2TextBlock}">
                         <Run Text="Номер телефона: "/>
                         <Run Text="{Binding UserProfile.Phone}"/>
                         <Run Text="День рождения: "/>
                         <Run Text="{Binding UserProfile.Birthday, StringFormat={}{0:dd.MM.yyyy}}"/>
                        </TextBlock>
                        <TextBlock>
                        <Run Text="Роль: "/>
                        <Run Text="{Binding Role.Title}"/>
                        </TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>

